[global]
    # font = Fira Mono 12
	font = ypn envypn 12

    allow_markup = yes

    # The format of the message.  Possible variables are:
    #   %a  appname
    #   %s  summary
    #   %b  body
    #   %i  iconname (including its path)
    #   %I  iconname (without its path)
    #   %p  progress value if set ([  0%] to [100%]) or nothing
    # Markup is allowed
    format = "<b>%a: %s</b>\n\n%b"
    sort = yes
    indicate_hidden = yes
    alignment = left
    bounce_freq = 0
    show_age_threshold = 60
    word_wrap = yes
    ignore_newline = no
    geometry = "800x15-30+60"
    shrink = yes
    transparency = 0
    idle_threshold = 120
    monitor = 0
    follow = mouse
    sticky_history = yes
    history_length = 20
    show_indicators = yes
    line_height = 0
    separator_height = 8
    padding = 64
    horizontal_padding = 22
	corner_radius = 8

    separator_color = "{color8}"

	startup_notification = false
    # dmenu = /usr/bin/dmenu -p dunst:
    browser = /usr/bin/google-chrome

	# Align icons left/right/off
    icon_position = left

	# Paths to default icons.
    icon_path = /usr/share/icons/Papirus-Dark/64x64@2x/panel/:/usr/share/icons/Papirus-Dark/64x64@2x/devices/:/usr/share/icons/Papirus-Dark/64x64@2x/apps:/usr/share/icons/Papirus-Dark/64x64@2x/actions:/usr/share/icons/Papirus-Dark/symbolic/status

    # Limit icons size
    # max_icon_size=64

    # icon_folders = /usr/share/icons/gnome/16x16/status/:/usr/share/icons/gnome/16x16/devices/

[frame]
    width = 2
    color = "#ebdbb2"

[shortcuts]
    close = ctrl+space
    close_all = ctrl+shift+space

[urgency_low]
	background = "{background}"
	foreground = "{color6}"
	timeout = 3

[urgency_normal]
	background = "{background}"
	foreground = "{color2}"
	timeout = 5

[urgency_critical]
	background = "{background}"
	foreground = "{color1}"
	timeout = 0

[thunderbird_ignore]
    appname = Thunderbird
    body = "*Connection to server*"
    format = ""

